/** Класс клиента (пользователя). */
export class User {
  /**
   * Конструктор класса клиента (пользователя).
   * @param {number} id 
   * @param {string} name 
   */  
  constructor(public id: number, public name: string) {}
}